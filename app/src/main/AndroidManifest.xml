<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.buftas.myPasswords">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".NoteActivity"
            android:label="New Note"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CreditCardActivity"
            android:label="@string/new_creditCard_activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PasswordActivity"
            android:label="@string/new_password_activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PasswordResetActivity"
            android:label="@string/password_reset_activity" />
        <activity
            android:name=".VaultManagementActivity"
            android:label="@string/personal_vault_activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/register_activity_title"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:label="@string/login_activity_title"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>